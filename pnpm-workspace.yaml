# Generated by scripts/reflex-os-workspace.mjs. Do not edit by hand.
#
# The public mirror's workspace. It carries the same catalog entries as the
# private repository so the exported sdk/ manifests resolve unchanged, which
# is what lets the mirror build and publish what this repository developed.
packages:
  - sdk/*

catalog:
  '@tanstack/react-query': ^5.101.4
  '@types/node': ^24.10.1
  '@types/react': ^19.2.18
  react: ^19.2.8
  react-markdown: ^10.1.0
  remark-gfm: ^4.0.1
  tsx: ^4.23.5
  typescript: ~5.9.3
  vite: ^7.3.1
  vitest: ^4.1.10
  zod: ^4.4.3

# sdk/tui bundles itself with esbuild, which needs its postinstall to fetch
# the platform binary. Without this pnpm silently skips it and the build
# fails on a missing binary rather than on anything obvious.
allowBuilds:
  esbuild: true
