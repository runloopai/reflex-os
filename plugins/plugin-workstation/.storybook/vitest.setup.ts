// Plugin Storybook vitest setup. The shared body lives in
// `@reflex/ui/storybook-preset/vitest-setup` so the animation kill-switch
// and `getElementError` configuration stay in lock-step with the host
// `web` Storybook.
import '@reflex/ui/storybook-preset/vitest-setup';
