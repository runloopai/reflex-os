# Reflex server origin (no /api suffix).
VITE_REFLEX_BASE_URL=http://localhost:4000

# Personal API key. Mint one in the Reflex web app under Security > API keys.
VITE_REFLEX_API_KEY=rfx_...

# Organization id (org_...) or slug the key's user belongs to.
VITE_REFLEX_ORG=my-org
